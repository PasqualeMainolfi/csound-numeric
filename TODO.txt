1. CREATION:
    X empty
    X zeros
    X ones
    X full
    X empty_like
    X zeros_like
    X ones_like
    X full_like
    X arange
    X linspace
    X logspace
    X geomspace
    X identity (see matrix)
    - random
    - randint

2. SHAPE
    X size
    X dims
    X shape
    X reshape
    X flatten
    X transpose
    X flip
    X roll
    X rollaxis

3. INDEXING
    X get (-> get at index) (1-D)
    X set (1-D)
    X take (N-D)
    X slice (N-D)
    X set_slice (N-D)
    X push (1-D)
    X pop (1-D)
    X insert (N-D)
    X remove (N-D)
    X concat
    X pad
    X clip
    X argwhere -> return index in 2D arr -> [[index, ndim], ...]
    X argnonzero -> return index in 2D arr
    X argunique -> return index in 2D arr
    X argisnan -> return index in 2D arr
    X argmin
    X argmax
    X greaterthan (gt) -> return value in 1D arr
    X lessthan (lt) -> return value in 1D arr
    X notequal (ne) -> return value in 1D arr
    X unique (uq) -> return value in 1D arr
    X countequal (eq) -> return value in 1D arr
    X countnonzero (nz) -> return value in 1D arr
    X countnan (nn) -> return value in 1D arr

4. REDUCTION
    X sum
    X prod
    X sub
    X mean
    X median
    X min
    X max
    X std
    X var
    X all
    X any

5. ELEMENTS
    X add
    X subtract
    X multiply
    X divide
    X power
    X sqrt
    X cbrt
    X abs
    X exp
    X log
    X sin
    X cos
    X tan
    X floor
    X ceil
    X round

6. VECTIORIAL
    X dot
    X inner
    X outer
    X norm
    X normalize
    X distance
    X angle
    X wrap_angle
    X project
    X reject
    X reflect
    X cross (only 3D)

7. NUMERIC ANALYSIS
    X diff
    X gradient
    X cumsum
    X cumprod

8. REALTIME STATS
    X movmean
    X movmedian
    X movstd
    X movvar
    X movemin
    X movemax

8. MATIRX
    X matmul
    X transpose
    X trace
    X diag
    X identity

9. ADD COMPLEX OPERATIONS
    costruzione/conversione:
        fromRealToComplex
        fromComplexToReal
        real(z)
        imag(z)
        conj(z)

    misure:
        abs(z)        -> modulo
        angle(z)      -> fase
        wrap_angle(z)
        norm(z)       -> eventualmente riusa abs per scalare

    aritmetica:
        add
        subtract
        multiply
        divide
        power

    funzioni:
        sqrt
        exp
        log
        sin
        cos
        tan

    coordinate:
        polar(abs, angle) -> complex
        to_polar(z)       -> modulo + fase

10. ADD K-RATE MODE
